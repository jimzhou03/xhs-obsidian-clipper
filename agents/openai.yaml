interface:
  display_name: "XHS Obsidian Clipper"
  short_description: "Archive full Xiaohongshu sources into Obsidian"
  default_prompt: "Use $xhs-obsidian-clipper to capture Xiaohongshu posts with body text, local images, loaded comments, resumable manifests, and an Obsidian synthesis."

policy:
  allow_implicit_invocation: true
